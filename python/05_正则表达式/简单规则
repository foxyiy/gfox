描述类型：
[]表示可选里面的元素
\d 等同于 [0-9]表示数字
\D 等同于 [^0-9]表示非数字
\w 等同于 [a-z0-9A-Z]
\W 等同于 [^a-z0-9A-Z]
.  表示任意一个字符
\s 匹配空格字符包括空格\n \t \r tab 等
\S 匹配非空格字符 
表示数量：
* :表示出现0或多次 即可有可无 当为0时匹配到的为空字符
+ 匹配前一个字符一次或多次 即最少出现一次
? 匹配前一个字符0或一次
{m-n}匹配前一个字符m-n次
{m.}匹配前一个字符至少m次
{m}匹配前一个字符m次
表示边界：
^匹配字符串开头
$匹配字符串结尾
\b 匹配一个单词的边界
\B 匹配非单词的边界
